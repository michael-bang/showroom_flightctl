= 2. Edge Manager - Manage

Your device has been successfully onboarded to Edge Manager and is operational. However, we need to add an application and config to it to make it more than just a device.

While we *could* edit this device individually and add an application directly to it, you can also create a `lab-fleet` that defines the desired state configuration (including an application) for any device that joins the fleet.

Rather than managing devices one at a time, we’ll use the fleet model to apply configuration consistently and at scale.

[#fleetcreation]
== Fleet Creation

Let's create a fleet that we can use to add the correct version of our operating system (image mode image), add an application, set the message of the day and have the fleet monitor
a systemd service.

. Return to your Edge Manager UI browser tab and Click *Fleets*
+
image::m2-create-fleet-button.png[link=self, window=blank, width=100%]
+
. Click on the *Create fleet* button
+
. Name the fleet lab-fleet
. Click on *Add label* under *Device selector* and write *fleet=lab-fleet* and press *Enter*.
+
image::m2-create-fleet-page1.png[link=self, window=blank, width=100%]
+
NOTE: You will see a message saying *There are no devices matching these labels*. When we enrolled our lab-srv1 device we didn't add any labels so this Fleet will not manage
any devices yet. We will come back later to our lab-srv1 device and add the label so that it will be managed by this fleet.
+
. Click *Next* to continue.

[#fleetconfiguration]
=== Fleet Configuration

The first change we can do through the Fleet is to update the operating system with a version of an image mode image. This feature could
be used to have one image installed on the device when you intially provision it eg. by your hardware dealer and then change it to the version
with the needed tools through the Fleet configuration. In this case we will keep the same image as is already installed on the device.

. Just leave the *System image* field empty

You can add or change any files on the device. We will update the Message Of The Day on the device through the Fleet to have them all reflect that technology
are managed through the Edge Manager.

image::m2-create-fleet-image.png[link=self, window=blank, width=100%]

. Click *Add configuration*
. Put in *update-motd* in the empty field 
. Select *Inline configuration* in the *Source type*
. Set the file path on the device to */etc/motd*
. In the content field enter *This system is managed by flightctl/rhem*
+
image::m2-create-fleet-configuration.png[link=self, window=blank, width=100%]
+
We will come back to adding an application later.
+
. Leave the rest as it is and press the *Next* button

[#fleetupdates]
=== Fleet Update policies

The updates part of the Fleet configuration provides you with functionality to *Use basic configuration* which will roll out any change
to the Fleet as soon as the edge manager agent does its next update. This is great if you want to roll out updates as soon as possible, but might
not be what you want to do in production.
If you unselect the *Use basic configuration* you can do more advanced configurations for your roll out. With the advanced configuration you can: *set roll out policies,
set disruption budget and set update policies*

The *set rollout policies* make it possible to select the order of the roll out meaning that you can have some device being updated first and only if
that goes well continue to the rest. 

image::m2-create-fleet-set-rollout-policies.png[link=self, window=blank, width=100%]

The *set disruption budget* ensure that you will not update all devices at once and if problems occur having a minimum set of available running devices

image::m2-create-fleet-set-disruption-budget.png[link=self, window=blank, width=100%]

The *set update policies* make it possible to define a schedule for when the updates should be applied.

image::m2-create-fleet-set-update-policies.png[link=self, window=blank, width=100%]

We will stick to the *basic configuration* in this lab.

image::m2-create-fleet-updates.png[link=self, window=blank, width=100%]

. Click the *Next* button

You can now review the *Fleet configuration*.

image::m2-create-fleet-review.png[link=self, window=blank, width=100%]

. Click the *Create fleet* button

Configurations, you now have a Fleet that is ready to update the Message Of The Day on a large set of devices. 

image::m2-lab-fleet-created.png[link=self, window=blank, width=100%]

[#fleetmembership]
== Fleet Membership 

In this section you will be adding the *edgesrv2* device to the *lab-fleet*.

. Return to your Edge Manager and Click *Devices*
+
image::m2-device-list.png[link=self, window=blank, width=100%]
+
. Click on the 3 dots next to the *edgesrv2*
+
image::m2-edgesrv2-device1.png[link=self, window=blank, width=100%]
+
. And press the *Edit device configurations*
+
image::m2-edgesrv2-device2.png[link=self, window=blank, width=100%]
+
. Click *Add label* under Device labels
. Replace the *key=value* with *fleet=lab-fleet*
+
image::m2-edgesrv2-device3.png[link=self, window=blank, width=100%]
+
You can now see that the label has ensured that the device will become part of the *lab-fleet* that you created earlier in this module
+
. Click *Next* to come to the *Review and Save* tab
+
image::m2-edgesrv2-device4.png[link=self, window=blank, width=100%]
+
You can see that a device label has been added to the fleet configuration.
+
. Click *Save* to finish the update
+
The update status of the device will change from *Out-of-date*
+
image::m2-edgesrv2-device5.png[link=self, window=blank, width=100%]
+
To *Updating* and then *Up-to-date*
+
[NOTE]
====
The agent on the device has checked in. Because the device’s *current configuration* does not match the *desired state* defined by the fleet specification, Edge Manager automatically instructs the device to reconcile itself.

This reconciliation is initiated by the device agent and occurs without manual intervention.
====
+
image::m2-edgesrv2-device6.png[link=self, window=blank, width=100%]
+
In the *Configurations* section of the *edgesrv2* you will now see that *update-motd* is now applied to the device
+
image::m2-edgesrv2-device7.png[link=self, window=blank, width=100%]
+
Lets check that the *Message Of The Day* has been updated on the device
. Click on the *Terminal* tab of the device
. Type *cat /etc/motd* and press *Enter*
+
image::m2-edgesrv2-device8.png[link=self, window=blank, width=100%]
+
The *Message Of The Day* has been successfully updated on the device.

[#fleetoverview]
=== Fleet overview

One thing is that we can see the update from the device standpoint, but if we have a lot of devices it would be great to see the information
from the stand point of the *Fleet*

. Click *Fleets* and then click on the *lab-fleet*
+
image::m2-fleet1.png[link=self, window=blank, width=100%]
+
From the overview there is a number of information that we can see. 
First we have the source. Here is the listed the sources that are being. In our case it is the *update-motd*.
Second we can see how many devices of the total amount in the *Fleet* that is *Up-to-date*. In our case the one device that is part of the
fleet is up-to-date. 
There is also three charts showing *Application status*, *Device status* and *Update status*

NOTE: Sources are different applications and configurations that are synchronized to the devices in the *Fleet*

[#addingapplication]
=== Adding an application 
Although updating *Message Of The Day* is great and shows that we can manage files at scale on a number of devices it would be great
to be able to add an application to the device.

Applications can be added through a Fleet in a number of ways. We will be adding a container based application to the device through the Fleet in 
This section.

NOTE: If you want to add a RPM based application to a device then you need to update the Image Mode image with the new application and distribute that through the operating system update
part that we skipped in the prior section




+
.Verify that the lab container is running
[source,sh,role=wrap execute,subs=attributes+]
----
podman ps -a 
----
+
.Sample Output
[source,text]
----
CONTAINER ID  IMAGE                           COMMAND         CREATED        STATUS        PORTS                   NAMES
d5fbd7c5e120  quay.io/kenosborn/drone-cam:v1  node server.js  9 minutes ago  Up 9 minutes  0.0.0.0:8081->8081/tcp  systemd-drone-cam-app-233433-app
----
+
.Verify that the lab image is present on the device
[source,sh,role=wrap execute,subs=attributes+]
----
podman images
----
+
.Sample Output
[source,text,role=wrap]
----
REPOSITORY                   TAG         IMAGE ID      CREATED      SIZE
quay.io/kenosborn/drone-cam  v1          7113794a637a  5 weeks ago  170 MB
----

Perfect! It looks like the device has reconciled to the desired state defined by the fleet configuration (including system configuration and application deployment) with the only manual step in this lab being the application of the fleet selector label.

Let's see if our Drone Cam is viewable now.

. Return to your drone cam route browser window and click *Reload*

#image::m3-join-fleet-5.png[link=self, window=blank, width=100%]

(You should now see the drone camera application load successfully)

Great job!  You have successfully stood up a new SDS field location, and your virtual pizza delivery is on its way (somewhere in the Greek Islands, by the look of it).

Now that this device is onboarded and enforcing fleet-defined state, we’ll use it to explore a few additional Edge Manager concepts.

=== Observability

NOTE: In addition to Events and Alerts, Edge Manager supports Observability capabilities for device and service *metrics, profiling, tracing, and audit logs*. This data can be stored over time to enable historical analysis and long-term insights.

Configuring Observability is beyond the scope of our lab; however, at a high level the following describes this functionality and flow: +

* Uses OpenTelemetry Collector on a device and forwards collected data to the Telemetry Gateway +
* Telemetry Gateway saves collected data from devices and service, or forwards data to external Collector +
* Standalone observability stack is available for Edge Manager on RHEL and/or the Observability stack in OpenShift can be utilized

[#repositories]

== Edge Manager Repositories

All configuration in Edge Manager around Fleets and the Software Catalog can be managed through Git and there is a CLI based tool that can be used to work with Edge Manager
without the use of the GUI. All configuration in Edge Manager can also be done with the CLI.

This lab does not include any hands on part in regards to this topic, but if you have a look in the *Fleets* section you will find a fleet
called *my-fleet*. This Fleet is managed through Git, so any changes done in Git will result in an update of the Fleet corresponding 
to the rollout policies defined for the Fleet.

In the *Repository* section you can see a repository called *fleets*. This repository include the nessesary information to setup the connection
between Edge Manager and Git. The repository also include a *Resource sync* for the fleet. You need one Resource sync for each Fleet definition
you want to synchronize through Git.

[#module2summary]
== Module 2 Summary

Thank you for completing *Module 2*.

In this module, we have created a Fleet and added one device into it so we can manage it. Then we added MOTD configuration, an application and
we set up a monitor of a systemd service. 

All of what we have done through out this module can also be done through git. This means that you can update your *Fleet* of devices 
through updates of Git. There is an example available here:

https://github.com/michael-bang/flightctl-demo/blob/main/simple/deployment/fleet.yaml

=== Up Next

In the next module, we’ll continue our test drive by working with the *Catalog* feature where we can provide applications with advanced updated paths. These catalog items can then 
be added to *Fleets* or *Devices* directly. 